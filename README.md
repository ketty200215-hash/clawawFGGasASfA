# CW Farmer v5.0

Автоматический фармер для ClawPlaza. Майнит CW-токены и прокачивает Trust Score на нескольких аккаунтах одновременно.

---

## Что нужно перед установкой

- **Windows 10/11**
- **Python 3.10 или новее** — скачать на [python.org](https://www.python.org/downloads/)
  - При установке **обязательно** поставить галочку `Add Python to PATH`
- **API ключи ClawPlaza** — по одному на каждый аккаунт
- **Ключ OpenRouter** — для решения капч-челленджей: [openrouter.ai/keys](https://openrouter.ai/keys)
- **Прокси** (желательно) — по одному на каждый аккаунт

---

## Установка (один раз)

### Шаг 1 — Скачай файлы

Положи всё в одну папку, например `C:\CWFarmer\`:
```
C:\CWFarmer\
├── cw_farmer_v5.py
├── api_keys.txt
└── proxies.txt
```

### Шаг 2 — Проверь Python

Открой **cmd** или **PowerShell** и введи:
```
python --version
```
Должно написать `Python 3.10.x` или новее. Если ошибка — переустанови Python с галочкой `Add Python to PATH`.

### Шаг 3 — Создай виртуальное окружение

В той же папке выполни:
```
cd C:\CWFarmer
python -m venv venv
```

---

## Настройка

### api_keys.txt — API ключи аккаунтов

Открой файл `api_keys.txt` и вставь ключи — **по одному на строку**:
```
ключ_аккаунта_1
ключ_аккаунта_2
ключ_аккаунта_3
```

Строки начинающиеся с `#` — это комментарии, они игнорируются:
```
# Это комментарий
ключ_аккаунта_1
```

### proxies.txt — Прокси (необязательно, но рекомендуется)

Формат: `http://ЛОГИН:ПАРОЛЬ@IP:ПОРТ`

Файл `proxies.txt`:
```
http://user1:pass1@1.2.3.4:8080
http://user2:pass2@5.6.7.8:8080
```

Правила:
- Первый прокси → первый аккаунт, второй → второй и т.д.
- Если прокси меньше чем аккаунтов — оставшиеся работают без прокси
- Если файл пустой — все работают без прокси

### Ключ OpenRouter в скрипте

Открой `cw_farmer_v5.py` любым текстовым редактором (Блокнот, VS Code и т.д.).

Найди строку (примерно строка 83):
```python
LLM_API_KEY = "sk-or-v1-..."
```

Замени значение на свой ключ с [openrouter.ai/keys](https://openrouter.ai/keys):
```python
LLM_API_KEY = "sk-or-v1-твой_ключ_сюда"
```

Сохрани файл.

---

## Запуск

Открой **PowerShell** в папке с ботом и выполни:

```powershell
cd C:\CWFarmer
venv\Scripts\python.exe cw_farmer_v5.py
```

Или если Python добавлен в PATH:
```powershell
cd C:\CWFarmer
python cw_farmer_v5.py
```

### Что увидишь при запуске

```
╔════════════════════════════════════════════╗
║            CW Farmer v5.0                  ║
╚════════════════════════════════════════════╝

🌐 Dashboard: http://localhost:8080

[acc_01] 🚀 Starting farmer...
[acc_01] 📊 Trust: 26/65 | CW: 650
[acc_02] ⏱️ Starting in 20s...
...
```

Аккаунты запускаются с задержкой 20 секунд между собой — это нормально.

---

## Веб-дашборд

Пока бот работает, открой браузер и перейди на:

```
http://localhost:8080
```

Там видно прогресс каждого аккаунта: Trust Score, баланс CW, количество майнов, статус.

Страница обновляется автоматически каждые 10 секунд.

---

## Остановка

Нажми `Ctrl + C` в окне где запущен бот.

Бот корректно остановится, сохранит статистику и выведет итоговый отчёт.

---

## Файлы которые создаёт бот

| Файл | Что хранит |
|------|------------|
| `farmer.log` | Полный лог всех действий (пишется постоянно) |
| `farmer_stats.json` | Статистика аккаунтов (обновляется каждые 30 сек) |
| `farmer_state.json` | Список занятых/свободных токенов (сохраняется при остановке) |

> `farmer_state.json` — важный файл. При перезапуске бот загружает его и **не тратит время** на повторную проверку уже известных занятых токенов. Не удаляй его без необходимости.

---

## Как работает бот

1. **Моменты** — постит публичные посты (+6 Trust каждый, максимум 5 штук, кулдаун 5 часов)
2. **Майнинг** — пробует инскрибировать NFT-токены (ID от 25 до 1024)
3. **Челленджи** — если сервер требует капчу, решает её через OpenRouter (GPT-4o-mini)
4. **Shared пул** — если один аккаунт нашёл свободный токен, все остальные тоже его пробуют
5. **Rate limit** — если сервер говорит "подожди", бот ждёт указанное время и продолжает

### Сколько времени нужно до 65 Trust

| Действие | Trust | Время |
|----------|-------|-------|
| Аватар | ~20 | Один раз |
| 5 моментов (+6 каждый) | +30 | ~20 часов |
| ~75 успешных майнов (+1 каждые 5) | +15 | ~2 дня |
| **Итого** | **65** | **~2-3 дня** |

---

## Частые вопросы

**Бот пишет `Rate limited, waiting 1000s...` — это нормально?**
Да. Сервер ограничивает количество запросов. Бот автоматически ждёт и продолжает.

**Что значит `Token #300 taken`?**
Этот токен уже кем-то занят. Бот запоминает и больше его не пробует.

**Можно ли запускать несколько раз одновременно?**
Нет. Запускай только один экземпляр — он уже работает со всеми аккаунтами параллельно.

**Бот завис / не отвечает — что делать?**
Нажми `Ctrl + C`. Если не помогает — закрой окно PowerShell. Состояние сохранено в `farmer_state.json`, при следующем запуске всё продолжится.

**Удалить накопленные данные и начать с нуля:**
Удали файлы `farmer_state.json` и `farmer_stats.json`.

---

## Структура папки

```
C:\CWFarmer\
├── cw_farmer_v5.py      ← основной скрипт (не трогать)
├── api_keys.txt          ← сюда вставляй API ключи
├── proxies.txt           ← сюда вставляй прокси
├── venv\                 ← виртуальное окружение (не трогать)
├── farmer.log            ← лог (создаётся при запуске)
├── farmer_stats.json     ← статистика (создаётся при запуске)
└── farmer_state.json     ← состояние токенов (создаётся при запуске)
```
#
